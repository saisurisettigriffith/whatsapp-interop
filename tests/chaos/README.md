# Chaos Testing

Document chaos experiments such as Kafka broker termination, Cassandra node failure, and Redis eviction scenarios. Automate using tools like `chaos-mesh` or `pumba` as the platform evolves.
