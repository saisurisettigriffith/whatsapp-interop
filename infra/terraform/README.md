# Terraform Stubs

Use Terraform to provision supporting infrastructure (S3 buckets, Kafka topics, Redis clusters, OpenSearch domain, etc.). Define modules per environment and integrate with CI/CD once ready.
